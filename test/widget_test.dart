import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder — real integration tests live in integration_test/
    expect(true, isTrue);
  });
}
